<form method="post">
    {{ csrfInput({ async: true }) }}
    {{ actionInput('entries/save-entry') }}
    {{ successMessageInput('Entry posted') }}
    {{ failMessageInput('Something went wrong.') }}
    {{ redirectInput('/guestbook') }}
    {{ hiddenInput('sectionId', id) }}
    {{ hiddenInput('title', 'Post ' ~ (now|date('Y-m-d H:i:s'))) }}

    <div class="mt-4 mb-6">
        <label for="post" class="font-bold">Message</label>
        <textarea
            name="fields[textBlock]"
            class="w-full px-6 py-4"
            required
            id="message"
        ></textarea>
    </div>
    <input
        type="submit"
        class="rounded bg-red-600 px-6 py-4 font-bold text-slate-50"
        value="Post entry"
    />
</form>
